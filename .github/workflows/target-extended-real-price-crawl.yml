name: Target extended real food-price crawl

on:
  push:
    branches:
      - food-real-scrape-20260811
    paths:
      - 'real_price_scrape/target_extended_crawl.py'
      - '.github/workflows/target-extended-real-price-crawl.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install runtime
        run: |
          python -m pip install --upgrade pip
          pip install httpx==0.28.1
      - name: Run deeper food-only local-price crawl
        run: python real_price_scrape/target_extended_crawl.py
      - name: Show manifest and file sizes
        if: always()
        run: |
          find real_food_price_output -maxdepth 3 -type f -printf '%p\t%s bytes\n' | sort || true
          find real_food_price_output -name manifest.json -exec cat {} \; || true
      - name: Upload extended corpus
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: utsa-madera-target-extended-real-food-prices
          path: real_food_price_output/
          if-no-files-found: warn
          retention-days: 7
